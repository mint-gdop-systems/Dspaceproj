### KOHA_CLIENT_ID / KOHA_CLIENT_SECRET
Generated in Koha Admin → OAuth2 clients when you create an OAuth2 client
(Grant type: Client credentials)
### Environment Variables

Add the following variables to the `.env` file:

```env
KOHA_CLIENT_ID=
KOHA_CLIENT_SECRET=
DSPACE_USERNAME=
DSPACE_PASSWORD=
